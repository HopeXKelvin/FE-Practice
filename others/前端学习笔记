2015-10-09
------jQuery脚本的结构------
jQuery的核心函数，通常是指一个工厂对象，即jQuery()函数，或者更为常用的别名$()函数。

------jq的工具函数-------
jq对象具有一些非常有用的方法，简称为工具方法。工具方法可以增强对于数组，对象，函数甚至数据的操作。
--1.对象
	$.type()
	$.isEmptyObject()
	$.extend()
--2.函数
	$.isFunction()
	$.noop()
--3.数组操作
	$.isArray()
	$.makeArray()
	$.merge()
	$.inArray()
	$.unique()
--4.数据结构
	
--5字符串
	$.trim()
--6.数据
	ajax

